<div class="payment-options">
  <!-- Bouton Stripe -->
  <%= button_to "Payer par Carte",
      checkout_create_path(
        amount: @amount,
        currency_type: @currency_type
      ),
      class: "btn btn-primary",
      data: { turbo: false }
  %>

  <!-- Bouton Metamask -->
  <button onclick="connectMetamask()" class="btn btn-metamask">
    Payer avec Metamask
  </button>

  <!-- Bouton Coinbase -->
  <button onclick="connectCoinbase()" class="btn btn-coinbase">
    Payer avec Coinbase
  </button>

  <!-- Bouton Binance -->
  <button onclick="connectBinance()" class="btn btn-binance">
    Payer avec Binance
  </button>
</div>

<script>
  async function connectMetamask() {
    if (typeof window.ethereum !== 'undefined') {
      try {
        // Demander la connexion au wallet
        const accounts = await window.ethereum.request({ method: 'eth_requestAccounts' });
        const account = accounts[0];

        // Ici, logique pour effectuer le paiement
        // ...
      } catch (error) {
        console.error(error);
      }
    } else {
      alert('Veuillez installer Metamask');
    }
  }

  // Fonctions similaires pour Coinbase et Binance
</script>
